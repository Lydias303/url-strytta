<p>Urls</p>
</div>
<div class="well">
  <div class="col"
<% @urls.each do |f| %>
  <%= f.title %>
  <%= f.clicks %>
  <%= link_to f.shortened_url, url_path(f.id) %><br>
<% end %>
<div class="search-field">
  <form>
    <input type="text" id="text_box" />
  </form>
</div>
</div>
